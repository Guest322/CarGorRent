<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <div class="container-fluid">
            <!-- Logo -->
            <a class="navbar-brand" href="/">CargoApp</a>
    
            <% if (user) {%>
            <!-- Navbar Toggle Button for Mobile -->
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
    
            <!-- Navbar Links -->
            <div class="collapse navbar-collapse" id="navbarNav">
                <!-- This will be your new navbar between the logo and login button -->
                <ul class="navbar-nav mx-auto">
                    <% if (user.role === 'super_admin') { %>
                        <li class="nav-item">
                          <a class="nav-link" href="/superadmin/createUser">Create User</a>
                        </li>
                        <li class="nav-item">
                          <a class="nav-link" href="/auth/superAdminDashboard">Super Admin Dashboard</a>
                        </li>
                      <% } else if (user.role === 'admin_sales') { %>
                        <li class="nav-item">
                          <a class="nav-link" href="/auth/adminDashboard">Admin Sales Dashboard</a>
                        </li>
                      <% } else if (user.role === 'customer') { %>
                        <li class="nav-item">
                          <a class="nav-link" href="/auth/customerDashboard">Customer Dashboard</a>
                        </li>
                      <% } %>
                </ul>
            </div>

            <% } else { %>
                <div>
                    <!-- <h1 class="nav-link mx-auto">Hallo</h1> -->
                </div>
            <% } %>
    
            <!-- Right-aligned login/register/logout buttons -->
            <ul class="navbar-nav">
                <% if (user) { %>
                    <li class="nav-item">
                        <a class="nav-link" href="/auth/logout">Logout</a>
                    </li>
                <% } else { %>
                    <li class="nav-item">
                        <a class="nav-link" href="/auth/login">Login</a>
                    </li>
                <% } %>
            </ul>
        </div>
    </nav>
    
    <div class="container mt-5">
        <%- body %>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
